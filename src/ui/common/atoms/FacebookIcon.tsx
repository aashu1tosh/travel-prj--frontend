import { FaFacebook } from 'react-icons/fa';

const FacebookIcon = () => {
    return (
        <span className='icons'>
            <FaFacebook size={25} />
        </span>
    );
};

export default FacebookIcon;
